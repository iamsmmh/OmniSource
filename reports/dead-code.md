# Dead Code Report

_Generated by `scripts/audit.py` — do not edit by hand; rerun `make audit` to refresh._

Files, globals, selectors and functions in the repository that nothing references.
Scope: committed source trees (`js/`, `src/`, `assets/`, `website/`, `docs/`, `sdk/`, `scripts/`)
plus the generated `feeds/` tree. Usage detection is textual: a name that appears in no
HTML attribute, JS/Python source, feed `icon` field or doc link is flagged. Textual scans
over-report dynamic usage, so every finding below lists the confirmation a maintainer
should do before deleting.

## Summary

- Unused files: **3**
- Dead JS globals: **0**
- Unreferenced CSS selectors: **0**
- Uncalled public Python functions: **0**

## Unused files

| File | Why it is flagged |
| --- | --- |
| `sdk/javascript/package.json` | no reference in HTML, JS, CSS, feed data or docs |
| `web/eslint.config.mjs` | no reference in HTML, JS, CSS, feed data or docs |
| `web/postcss.config.mjs` | no reference in HTML, JS, CSS, feed data or docs |

## Dead JS globals

- None — every `window.*` namespace is consumed.

## Unreferenced CSS selectors

None found across the design system.

## Dead Python functions

- None — every public function in `src/omnisource` has at least one caller or test.

## Verification checklist

- Before deleting a file: `rg <name>` across the whole tree (feed JSON included).
- Before deleting a selector: check class strings built at runtime (`cls + '-active'` etc.).
- Before deleting a Python function: run `make test` — tests may import it directly.
