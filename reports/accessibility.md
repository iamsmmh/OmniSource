# Accessibility Audit

_Generated by `scripts/audit.py` — do not edit by hand; rerun `make audit` to refresh._

Structural WCAG-AA checks run over every hand-maintained HTML page (generated app/collection/
source pages are scanned separately by the smoke test markers). Contrast, focus states and
motion policy live in the design system and are noted per section.

## Summary

- Pages scanned: **24**
- Pages with structural findings: **0**

## Findings by page

No structural findings — every scanned page declares `lang`, labels its controls, keeps heading order and guards `target=_blank`.

## Design-system guarantees

- `:focus-visible` styles: present
- `prefers-reduced-motion` respected in animations.css: yes; tokens.css: yes
- Contrast: status colors (`--green/--amber/--red`) pair with `-soft` surfaces on both themes; verify badge text ≥ 4.5:1 after any palette change (see `tokens.css`).
- Keyboard: ⌘K palette, dialogs via `<dialog>` + `showModal`, skip links, focus rings.

## Manual verification checklist

- [ ] Tab through the header nav, filters, palette and dialogs in dark + light themes.
- [ ] NVDA/VoiceOver pass on the home page (announced live regions update on filter).
- [ ] Toggle `prefers-reduced-motion` in dev tools: all transforms/orbs must stop.
- [ ] RTL (Arabic) layout of hero + nav + dialogs.
