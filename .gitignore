# Build output
_site/
*.tmp

# Legacy local API mirror (no longer written by the pipeline; /api/ is
# assembled inside _site/ by scripts/build_site.py). Kept ignored so stale
# checkouts with an old api/ directory stay clean.
api/

# Python tooling caches
__pycache__/
*.py[cod]
.ruff_cache/
.mypy_cache/
src/omnisource.egg-info/

# Local environment
.env
.venv/
.DS_Store

# Asset / HTTP cache (never commit IPA payloads; this dir is metadata only)
.cache/
