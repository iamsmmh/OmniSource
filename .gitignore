# Build output
_site/
*.tmp

# Local API mirror — byte-identical to feeds/*.json (regenerated by the
# pipeline and published as /api/ inside _site by scripts/build_site.py).
# Not committed: the flat feed copies at the repository root are the
# committed equivalent of this mirror (Jekyll build + CI deploy both serve them).
api/

# Python tooling caches
__pycache__/
*.py[cod]
.ruff_cache/
.mypy_cache/
src/omnisource.egg-info/

# Local environment
.env
.venv/
.DS_Store

# Asset / HTTP cache (never commit IPA payloads; this dir is metadata only)
.cache/
